<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="rule_certificate_request_portal_own" model="ir.rule">
        <field name="name">Portal: own certificate requests only</field>
        <field name="model_id" ref="model_univ_sms_certificate_request"/>
        <field name="domain_force">[('student_id.partner_id', '=', user.partner_id.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
    </record>
</odoo>
