<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_certificate_type_tree" model="ir.ui.view">
        <field name="name">univ.sms.certificate.type.tree</field>
        <field name="model">univ.sms.certificate.type</field>
        <field name="arch" type="xml">
            <tree>
                <field name="code"/>
                <field name="name"/>
                <field name="require_fee"/>
                <field name="fee_amount"/>
            </tree>
        </field>
    </record>

    <record id="view_certificate_type_form" model="ir.ui.view">
        <field name="name">univ.sms.certificate.type.form</field>
        <field name="model">univ.sms.certificate.type</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="code"/>
                        <field name="require_fee"/>
                        <field name="fee_amount" invisible="not require_fee"/>
                        <field name="description"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_certificate_type" model="ir.actions.act_window">
        <field name="name">Loại giấy chứng nhận</field>
        <field name="res_model">univ.sms.certificate.type</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>