<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="rule_feedback_portal_own" model="ir.rule">
        <field name="name">Portal: own feedback only</field>
        <field name="model_id" ref="model_univ_sms_feedback"/>
        <field name="domain_force">[('student_id.partner_id', '=', user.partner_id.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
    </record>
</odoo>
